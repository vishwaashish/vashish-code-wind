---
title: Loaders Gallary
description:
  Enhance User Experience and Aesthetics with Our Range of Creative CSS
  Loaders for Seamless Loading Animations
---

# Loaders Gallary

Enhance User Experience and Aesthetics with Our Range of Creative CSS Loaders for Seamless Loading Animations

<div className="preview-code mt-12 grid grid-cols-1 overflow-hidden sm:grid-cols-2 lg:grid-cols-3 [&>*]:relative [&>*]:px-1 [&>*]:py-12 [&>*]:before:absolute [&>*]:before:bg-border/70 [&>*]:before:[block-size:100vh] [&>*]:before:[inline-size:1px] [&>*]:before:[inset-block-start:0] [&>*]:before:[inset-inline-start:-1px] [&>*]:after:absolute [&>*]:after:bg-border/70 [&>*]:after:[block-size:1px] [&>*]:after:[inline-size:100vw] [&>*]:after:[inset-block-start:-1px] [&>*]:after:[inset-inline-start:0] sm:[&>*]:px-8 xl:[&>*]:px-12">
  {[
    "loader-01",
    "loader-02",
    "loader-03",
    "loader-04",
    "loader-05",
    "loader-06",
    "loader-07",
    "loader-08",
    "loader-09",
    "loader-10",
    "loader-11",
    "loader-12",
    "loader-13",
    "loader-14",
    "loader-15",
  ].map((componentName) => {
    return (
      <div
        // href={`/component/loaders/${componentName}`}
        key={componentName}
        className="group/item relative"
      >
        <DemoComponent
          key={componentName}
          directory={"loaders"}
          componentName={componentName}
          className="flex w-full items-center justify-center"
        />
      </div>
    );
  })}
</div>

## Code Example

<ComponentViewer
  directory="loaders"
  componentName={"loader-01"}
  isComponent
  previewClassName="grid place-content-center min-h-[200px]"
/>

## 🎨 Customization Guide

You can easily adjust the loader's appearance using the following CSS variables:

- `--loader-primary`: Adjusts the primary color of the loader. Use your theme's `--primary` variable.
- `--loader-secondary`: Controls the secondary color, usually used for backgrounds or accents.
- `--loader-border`: Sets the border thickness for the loader rings.
- `--loader-width`: Defines the overall size of the loader.
- `--loader-speed`: Controls the speed of the animation.

### Example Customization

```css
:root {
  --loader-primary: hsl(220, 90%, 56%);
  --loader-secondary: hsl(220, 20%, 80%);
  --loader-border: 3px;
  --loader-width: 64px;
  --loader-speed: 1.5s;
}
```

## Usage Instructions

1. Copy the CSS and place it in your stylesheet (global.css or relevant module
   file).
2. Use the HTML structure in your component or .mdx file.
3. Customize the variables (`--primary`, `--secondary`) to fit your theme.
