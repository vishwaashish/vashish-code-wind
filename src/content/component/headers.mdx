---
title: Responsive Header
description: A clean and responsive header component with a brand title and navigation links
---

# Responsive Header

<div className="mt-9 grid grid-cols-1 gap-10">
  {["header-01", "header-02"].map((componentName) => {
    return (
        <ComponentViewer 
        directory="headers"
          componentName={componentName}
            key={componentName}>

        </ComponentViewer>
    );

})}
</div>
