---
title: Input Examples
description: Examples demonstrating its flexibility, including integration with
  related components like Label and FormGroup for accessibility and
  structured layouts.
links:
  - type: docs
    url: /component/input
    label: Input
---

<div className="preview-code grid grid-cols-1 overflow-hidden sm:grid-cols-2 lg:grid-cols-3 [&>*]:relative [&>*]:px-1 [&>*]:py-10 [&>*]:before:absolute [&>*]:before:bg-border/70 [&>*]:before:[block-size:100vh] [&>*]:before:[inline-size:1px] [&>*]:before:[inset-block-start:0] [&>*]:before:[inset-inline-start:-1px] [&>*]:after:absolute [&>*]:after:bg-border/70 [&>*]:after:[block-size:1px] [&>*]:after:[inline-size:100vw] [&>*]:after:[inset-block-start:-1px] [&>*]:after:[inset-inline-start:0] sm:[&>*]:px-8 xl:[&>*]:px-12">
  {["input-03", "input-04", "input-05", "input-06"].map((componentName) => {
    return (
      <div key={componentName} className="group relative">
        <DemoComponent
          key={componentName}
          directory={"demos/inputs"}
          component={componentName}
          componentName="InputDemo"
          className="flex w-full items-center justify-center"
          copyClassName="opacity-0 group-hover:opacity-100"
        />
      </div>
    );
  })}
  {[
    "textarea-01",
    "textarea-02",
    "textarea-03",
    "textarea-04",
    "textarea-05",
  ].map((componentName) => {
    return (
      <div key={componentName} className="group relative">
        <DemoComponent
          key={componentName}
          directory={"demos/textareas"}
          component={componentName}
          componentName="TextareaDemo"
          className="flex w-full items-center justify-center"
          copyClassName="opacity-0 group-hover:opacity-100"
        />
      </div>
    );
  })}
  {[
    "input-group-01",
    "input-group-02",
    "input-group-03",
    "input-group-04",
    "input-group-05",
    "input-group-06",
    "input-group-07",
    "input-group-08",
  ].map((componentName) => {
    return (
      <div key={componentName} className="group relative">
        <DemoComponent
          key={componentName}
          directory={"demos/input-groups"}
          component={componentName}
          componentName="InputGroupDemo"
          className="flex w-full items-center justify-center"
          copyClassName="opacity-0 group-hover:opacity-100"
        />
      </div>
    );
  })}
</div>
